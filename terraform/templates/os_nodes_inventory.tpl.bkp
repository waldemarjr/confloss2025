[vpn_server]
${vpn_server_name} ansible_host=${vpn_server_ip} ansible_user=admin

[controller]
${os_controller_node_name} ansible_host=${os_controller_node_ip} ansible_user=admin

[compute]
${os_compute_node_name} ansible_host=${os_compute_node_ip} ansible_user=admin

[all:vars]
ansible_ssh_private_key_file: ${key_path}
